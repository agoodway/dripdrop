# add-dripdrop-foundation

Bootstrap the DripDrop library: schema-isolated multi-channel messaging sequence engine with PgFlow dispatch, encrypted DB-stored adapters, Elixir/HTTP hooks, branching transitions, suppression/consent policy, idempotent execution, and pluggable short-link providers.
